# Обработчик Webhook Avito - Инструкция по настройке

## ✅ Выполненные шаги

### 1. База данных PostgreSQL ✓
- **Статус**: Установлена и работает
- **Контейнер**: `postgres-avito`
- **База данных**: `avito_db`
- **Пользователь**: `avito_user`
- **Пароль**: `AvitoDB2026Secure!Pass`
- **Порт**: `5432`
- **Созданные таблицы**:
  - `avito_orders` (с индексами)
  - `avito_listings` (с индексами)
  - `webhook_errors`

### 2. JSON файл workflow ✓
- **Файл**: [Avito_Webhook_Processor.json](Avito_Webhook_Processor.json)
- **Узлов (nodes)**: 13 узлов (10 основных + 3 обработки ошибок)
- **Готов к импорту в n8n**

---

## 📋 Следующие шаги

### Шаг 1: Импорт Workflow в n8n

1. **Откройте n8n**: Перейдите на https://n8n.oknaprof59.ru
2. **Войдите в систему**: Используйте ваши учетные данные n8n
   - Email: `oknaprof59@gmail.com`
   - Пароль: `db2328addf` (если не менялся)

3. **Импортируйте Workflow**:
   - Нажмите кнопку **"+"** в правом верхнем углу
   - Выберите **"Import from File"** (Импорт из файла)
   - Выберите файл `Avito_Webhook_Processor.json`
   - Нажмите **"Import"**

### Шаг 2: Настройка учетных данных PostgreSQL

1. **Перейдите в Credentials** (Учетные данные):
   - Нажмите **Settings** (Настройки, значок шестеренки) в левой панели
   - Выберите **Credentials** (Учетные данные)
   - Нажмите **"Add Credential"** (Добавить учетные данные)

2. **Создайте учетные данные PostgreSQL**:
   - **Type** (Тип): PostgreSQL
   - **Name** (Название): `Avito PostgreSQL`
   - **Host** (Хост): `localhost` (или `postgres-avito` если в Docker сети)
   - **Database** (База данных): `avito_db`
   - **User** (Пользователь): `avito_user`
   - **Password** (Пароль): `AvitoDB2026Secure!Pass`
   - **Port** (Порт): `5432`
   - **SSL Mode** (Режим SSL): `Disable` (Отключено)
   - Нажмите **"Create"** (Создать)

3. **Проверьте подключение**:
   - Нажмите кнопку **"Test"** (Тест)
   - Должно появиться "Connection successful" (Подключение успешно)

### Шаг 3: Создание Telegram бота и настройка учетных данных

#### Создание Telegram бота:

1. **Откройте Telegram** и напишите боту [@BotFather](https://t.me/BotFather)
2. **Отправьте команду**: `/newbot`
3. **Следуйте инструкциям**:
   - Имя бота: `Помощник Avito` (или любое другое)
   - Username: `avito_oknaprof_bot` (должен заканчиваться на `_bot`)
4. **Скопируйте токен доступа** (выглядит так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. **Запустите бота**: Найдите вашего бота в Telegram и нажмите **"Старт"**

#### Получение вашего Chat ID:

1. **Отправьте сообщение** вашему боту (любое сообщение)
2. **Откройте браузер** и перейдите по ссылке:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
   Замените `<ВАШ_ТОКЕН>` на токен вашего бота
3. **Найдите ваш Chat ID** в ответе:
   ```json
   {
     "result": [
       {
         "message": {
           "chat": {
             "id": 123456789,  ← Это ваш Chat ID
             "first_name": "Ваше Имя"
           }
         }
       }
     ]
   }
   ```

#### Настройка в n8n:

1. **Перейдите в Credentials** (Учетные данные) в n8n
2. **Add Credential** (Добавить) → **Telegram**
3. **Настройки**:
   - **Name** (Название): `Avito Notifications Bot`
   - **Access Token** (Токен доступа): `<токен вашего бота>`
   - Нажмите **"Create"** (Создать)

### Шаг 4: Привязка учетных данных к узлам workflow

1. **Откройте импортированный workflow**
2. **Нажмите на каждый узел**, которому нужны учетные данные:

   **Узлы PostgreSQL** (4 узла):
   - `Check for Duplicates` (Проверка дубликатов)
   - `Insert New Order` (Вставка нового заказа)
   - `Update Existing Order` (Обновление существующего заказа)
   - `Log Error to Database` (Запись ошибки в БД)

   Для каждого узла:
   - Нажмите на узел
   - В выпадающем списке **"Credential to connect with"** (Учетные данные для подключения)
   - Выберите **"Avito PostgreSQL"**

   **Узлы Telegram** (2 узла):
   - `Telegram Notification` (Уведомление Telegram)
   - `Error Alert` (Оповещение об ошибке)

   Для каждого узла:
   - Нажмите на узел
   - Измените параметр **"Chat ID"**: Замените `YOUR_TELEGRAM_CHAT_ID` на ваш реальный Chat ID (число)
   - В выпадающем списке **"Credential to connect with"**
   - Выберите **"Avito Notifications Bot"**

3. **Сохраните workflow**: Нажмите кнопку **"Save"** (Сохранить) в правом верхнем углу

### Шаг 5: Активация Workflow

1. **Нажмите переключатель** в правом верхнем углу для активации
2. **Скопируйте URL webhook**: Должен показать:
   ```
   https://n8n.oknaprof59.ru/webhook/avito-marketplace
   ```
   Это ваш рабочий URL webhook

---

## 🧪 Тестирование

### Тест 1: Ручное тестирование Webhook

Используйте curl для отправки тестового webhook:

```bash
curl -X POST https://n8n.oknaprof59.ru/webhook/avito-marketplace \
  -H "Content-Type: application/json" \
  -d '{
    "event_type": "new_order",
    "timestamp": "2026-02-07T12:00:00Z",
    "data": {
      "order_id": "TEST_12345",
      "status": "pending",
      "total_amount": 5000,
      "items": [
        {
          "listing_id": "999",
          "title": "Тестовый товар",
          "price": 5000,
          "quantity": 1
        }
      ],
      "customer": {
        "name": "Тестовый покупатель",
        "phone": "+79991234567",
        "email": "test@example.com"
      }
    }
  }'
```

**Ожидаемые результаты**:
1. ✅ HTTP 200 ответ в течение 2 секунд
2. ✅ JSON ответ: `{"status":"received","webhook_id":"...","timestamp":"...","message":"Order processed successfully"}`
3. ✅ Получено уведомление в Telegram
4. ✅ Новая запись в базе данных

### Тест 2: Проверка записи в базу данных

Подключитесь к серверу по SSH и проверьте базу данных:

```bash
ssh root@83.219.243.108
docker exec -it postgres-avito psql -U avito_user -d avito_db -c \
  "SELECT id, external_id, total_amount, customer_name, created_at FROM avito_orders ORDER BY id DESC LIMIT 5;"
```

Должен показать ваш тестовый заказ с `TEST_12345`.

### Тест 3: Проверка логов выполнения n8n

1. Перейдите в интерфейс n8n: https://n8n.oknaprof59.ru
2. Нажмите **"Executions"** (Выполнения) в левой панели
3. Найдите последнее выполнение
4. Нажмите для просмотра деталей
5. Проверьте вывод каждого узла
6. Убедитесь, что нет ошибок

---

## 🔧 Устранение неполадок

### Проблема: Workflow не получает webhooks

**Проверьте**:
1. Workflow **активирован** (переключатель в правом верхнем углу)
2. URL webhook правильный: `https://n8n.oknaprof59.ru/webhook/avito-marketplace`
3. Контейнер n8n запущен: `docker ps | grep n8n`
4. Traefik правильно маршрутизирует запросы

### Проблема: Не удалось подключиться к базе данных

**Проверьте**:
1. Контейнер PostgreSQL запущен: `docker ps | grep postgres-avito`
2. Учетные данные в n8n правильные
3. Порт 5432 доступен
4. Попробуйте подключиться вручную:
   ```bash
   docker exec -it postgres-avito psql -U avito_user -d avito_db -c "SELECT 1;"
   ```

### Проблема: Уведомления Telegram не отправляются

**Проверьте**:
1. Токен бота правильный
2. Chat ID правильный (число)
3. Вы запустили бота (отправили `/start`)
4. Проверьте API вызов:
   ```bash
   curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getMe"
   ```

### Проблема: Ошибки дублирования ключей

Это ожидаемо и обрабатывается! Workflow проверяет дубликаты перед вставкой. Если вы видите это в логах, значит обнаружение дубликатов работает правильно.

---

## 📱 Регистрация Webhook в Avito

### Вариант 1: Личный кабинет Avito (если доступно)

1. Войдите в [Личный кабинет продавца Avito](https://www.avito.ru/professionals)
2. Перейдите в **Настройки** → **API** → **Webhooks**
3. Нажмите **"Добавить Webhook"**
4. **Настройки**:
   - **URL**: `https://n8n.oknaprof59.ru/webhook/avito-marketplace`
   - **События**: Выберите:
     - ✅ `new_order` (новый заказ)
     - ✅ `order_status_change` (изменение статуса заказа)
     - ✅ `listing_update` (обновление объявления) - опционально
     - ✅ `new_message` (новое сообщение) - опционально
   - **Secret** (Секрет): Сгенерируйте или создайте свой
5. Нажмите **"Сохранить"**
6. **Протестируйте webhook** из личного кабинета Avito

### Вариант 2: API Avito (если нет опции в кабинете)

Обратитесь в поддержку Avito для регистрации webhook программно через их API.

**Примечание**: Сохраните секрет webhook! Он понадобится для проверки подписей webhook (в данный момент не реализовано, но можно добавить).

---

## 📊 Мониторинг

### Ежедневные проверки

1. **Проверяйте уведомления Telegram** - убедитесь, что получаете оповещения
2. **Проверяйте выполнения n8n** - проверьте на наличие сбоев
3. **Мониторьте рост базы данных**:
   ```bash
   docker exec postgres-avito psql -U avito_user -d avito_db -c \
     "SELECT COUNT(*) as total_orders FROM avito_orders;"
   ```

### Еженедельное обслуживание

1. **Проверяйте логи ошибок**:
   ```bash
   docker exec postgres-avito psql -U avito_user -d avito_db -c \
     "SELECT * FROM webhook_errors ORDER BY occurred_at DESC LIMIT 10;"
   ```

2. **Проверяйте логи контейнера n8n**:
   ```bash
   docker logs n8n-n8n-1 --tail 100
   ```

3. **Проверяйте использование диска PostgreSQL**:
   ```bash
   docker exec postgres-avito du -sh /var/lib/postgresql/data
   ```

---

## 🚀 Следующие шаги (Фаза 2)

Как только базовый workflow будет стабильно работать, рассмотрите добавление:

1. **Обогащение данными из Avito API**:
   - Добавить узел HTTP Request для получения дополнительных деталей заказа
   - Требуются учетные данные Avito OAuth2

2. **Email уведомления**:
   - Добавить узел Email Send для заказов с высокой стоимостью

3. **Интеграция с внешним API**:
   - Перенаправление заказов в ERP/CRM систему

4. **Ветка обработки объявлений**:
   - Обработка событий `listing_update`
   - Сохранение в таблицу `avito_listings`

5. **Автоответы**:
   - Автоматический ответ на сообщения покупателей

6. **Аналитическая панель**:
   - Отслеживание объемов заказов
   - Мониторинг времени ответа

---

## 📝 Важные файлы

- **Workflow JSON**: [Avito_Webhook_Processor.json](Avito_Webhook_Processor.json)
- **План реализации**: [transient-prancing-sutton.md](.claude/plans/transient-prancing-sutton.md)
- **Данные сервера**: [НОВЫЙ_СЕРВЕР_ДАННЫЕ.md](НОВЫЙ_СЕРВЕР_ДАННЫЕ.md)
- **Документация проекта**: [avito_assist/Claud.md](avito_assist/Claud.md)

---

## ⚙️ Сводка конфигурации

| Компонент | Значение |
|-----------|----------|
| **URL Webhook** | `https://n8n.oknaprof59.ru/webhook/avito-marketplace` |
| **Экземпляр n8n** | `https://n8n.oknaprof59.ru` |
| **Хост PostgreSQL** | `localhost:5432` |
| **Имя базы данных** | `avito_db` |
| **Пользователь БД** | `avito_user` |
| **Пароль БД** | `AvitoDB2026Secure!Pass` |
| **IP сервера** | `83.219.243.108` |
| **SSH сервера** | `root@83.219.243.108` |
| **Пароль SSH** | `HbPFZDqCunT8Zz` |

---

## 📞 Поддержка

Если возникли проблемы:

1. Сначала проверьте эту инструкцию
2. Просмотрите [раздел устранения неполадок](#-устранение-неполадок)
3. Проверьте логи n8n: `docker logs n8n-n8n-1 --tail 100`
4. Проверьте логи PostgreSQL: `docker logs postgres-avito --tail 100`

---

## 🎯 Краткое руководство по запуску

**Для быстрого старта выполните эти 5 шагов:**

1. Откройте https://n8n.oknaprof59.ru и импортируйте `Avito_Webhook_Processor.json`
2. Создайте учетные данные PostgreSQL (хост: `localhost`, БД: `avito_db`, пользователь: `avito_user`, пароль: `AvitoDB2026Secure!Pass`)
3. Создайте Telegram бота через @BotFather, получите токен и Chat ID
4. Свяжите учетные данные со всеми узлами PostgreSQL и Telegram в workflow
5. Активируйте workflow и протестируйте с помощью curl команды выше

**Готово!** Ваш webhook готов принимать заказы от Avito! 🎉

---

**Статус**: ✅ База данных готова | ⏳ Workflow требует ручного импорта и настройки учетных данных | ⏳ Тестирование в ожидании

**Создано**: 2026-02-07
**Последнее обновление**: 2026-02-07
