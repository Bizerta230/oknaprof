# План работы с n8n - Продолжение

## Что сделано сегодня ✓

1. **Восстановлена конфигурация Claude Code**
   - Восстановлена история проекта n8n (4 сессии, включая большую на 394 сообщения)
   - Восстановлены файлы истории и настройки IDE

2. **Диагностика подключения к n8n**
   - Проверено состояние MCP сервера - работает корректно
   - Все 20 инструментов n8n MCP загружены и доступны
   - Выявлена проблема: сервер n8n.oknaprof59.ru не отвечает (404)

3. **Удален CLAWDBOT**
   - Остановлены процессы Node.js (освобождена память)
   - Удален глобальный пакет clawdbot
   - Очищен npm кэш

4. **Диагностика системы**
   - Проверены процессы, использующие память
   - Основные потребители: Edge, Chrome, VSCode (это нормально)

## Текущая конфигурация

**Файл:** `C:\Users\Анна\Dropbox\Мой ПК (LAPTOP-BV175C6S)\Desktop\n8n\.mcp.json`

```json
{
  "mcpServers": {
    "n8n-mcp": {
      "type": "stdio",
      "command": "cmd",
      "args": ["/c", "npx", "-y", "n8n-mcp"],
      "env": {
        "MCP_MODE": "stdio",
        "N8N_API_URL": "https://n8n.oknaprof59.ru",
        "N8N_API_KEY": "eyJhbG..."
      }
    }
  }
}
```

**Бэкап:** `.mcp.json.backup` (создан для безопасности)

## Данные для доступа к серверу

- **Хостинг:** https://my.hip.hosting/hiplets/65317
- **IP:** 193.168.196.225
- **Hostname:** n8n.oknaprof59.ru
- **SSH команда:** `ssh root@193.168.196.225`
- **Пароль SSH:** TCSug8nGTLdZ54
- **Email n8n:** oknaprof59@gmail.com
- **Пароль n8n:** db2328addf

## План на завтра 📋

### 1. Развернуть n8n на сервере заново

**Если используете Docker (рекомендуется):**

```bash
# Подключиться к серверу
ssh root@193.168.196.225

# Остановить старый контейнер если есть
docker stop n8n
docker rm n8n

# Запустить новый контейнер n8n
docker run -d \
  --name n8n \
  --restart unless-stopped \
  -p 5678:5678 \
  -e N8N_HOST=n8n.oknaprof59.ru \
  -e N8N_PROTOCOL=https \
  -e WEBHOOK_URL=https://n8n.oknaprof59.ru/ \
  -v n8n_data:/home/node/.n8n \
  n8nio/n8n

# Проверить что работает
docker ps
docker logs n8n
curl http://localhost:5678/healthz
```

### 2. Настроить reverse proxy (nginx)

Если n8n работает локально но не доступен извне, нужно настроить nginx:

```bash
# Проверить конфигурацию nginx
cat /etc/nginx/sites-enabled/* | grep n8n -A 20

# Если конфигурации нет, создать её
# (попросите Claude помочь с правильной конфигурацией nginx)
```

### 3. Получить/создать API ключ

После того как n8n заработает:

1. Откройте браузер: https://n8n.oknaprof59.ru
2. Войдите с данными: oknaprof59@gmail.com / db2328addf
3. Перейдите в: Settings → API
4. Создайте новый API ключ
5. Скопируйте его

### 4. Обновить конфигурацию MCP

Обновите `.mcp.json` с новым API ключом:

```json
{
  "mcpServers": {
    "n8n-mcp": {
      "type": "stdio",
      "command": "cmd",
      "args": ["/c", "npx", "-y", "n8n-mcp"],
      "env": {
        "MCP_MODE": "stdio",
        "N8N_API_URL": "https://n8n.oknaprof59.ru",
        "N8N_API_KEY": "НОВЫЙ_API_КЛЮЧ_СЮДА"
      }
    }
  }
}
```

### 5. Перезагрузить VSCode и проверить

```
1. Ctrl + Shift + P
2. "Reload Window"
3. Подождать 10 секунд
4. Сказать Claude: "проверь подключение к n8n"
```

## Команды для проверки на сервере

```bash
# Проверка Docker контейнеров
docker ps -a

# Логи n8n
docker logs n8n --tail 50

# Проверка что n8n отвечает локально
curl http://localhost:5678/healthz

# Проверка API
curl -H "X-N8N-API-KEY: ВАШ_API_КЛЮЧ" http://localhost:5678/api/v1/workflows

# Проверка портов
netstat -tlnp | grep 5678

# Перезапуск n8n
docker restart n8n

# Перезапуск nginx
systemctl reload nginx
```

## Альтернатива: Локальный n8n

Если проблемы с сервером продолжатся, можно быстро установить n8n локально:

```bash
# Установить n8n глобально
npm install -g n8n

# Запустить
n8n

# Откроется на http://localhost:5678
```

Затем обновить `.mcp.json`:
```json
"N8N_API_URL": "http://localhost:5678"
```

## Полезные ссылки

- Документация n8n: https://docs.n8n.io/
- n8n MCP GitHub: https://github.com/czlonkowski/n8n-mcp
- Хостинг панель: https://my.hip.hosting/hiplets/65317

---

**Примечание:** Все данные для доступа сохранены в этом файле. Завтра просто скажите Claude "продолжи работу с n8n" и он вспомнит весь контекст!
